# Placeholders only — no verified hostnames or paths. Fill in per-machine.

# C1 — capture / transcription
C1_HOST=127.0.0.1
C1_PORT=8001

# C2 — reasoning / RAG
C2_HOST=127.0.0.1
C2_PORT=8002

# C3 — interjection router
C3_HOST=127.0.0.1
C3_PORT=8003

# C4 — speech synthesis
C4_HOST=127.0.0.1
C4_PORT=8004

# C5 — orchestrator
C5_HOST=127.0.0.1
C5_PORT=8005

# Ollama endpoint + model tag (C2)
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=gemma4:e4b

# Kokoro TTS weights (C4)
KOKORO_WEIGHTS_PATH=/path/to/kokoro-v1.0.onnx
KOKORO_VOICES_PATH=/path/to/voices-v1.0.bin
